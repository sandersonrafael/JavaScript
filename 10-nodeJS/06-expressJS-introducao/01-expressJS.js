// inicia o projeto com npm init -y
// depois instala o express com o comando npm install express (pode usar ou não --save depois do comando)
// já vem como uma dependência do produto e não de desenvolvimento
// cria const express = require('express') no server
// cria const app = express() para executar o express
