let numeros = [1, 2, 3];  // Necessário ";" para esse caso  // inicializa e define o array
[a, b, c] = numeros;      // atribui para cada índice uma variável
console.log(numeros)


let d = 'D'
let e = 'E'
let f = 'F'

let letras = [e, f, d];
[d, e, f] = letras;
console.log(d, e, f);
